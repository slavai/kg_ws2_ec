/** 
 * Tailwind CSS v4 Configuration
 * Minimized for v4 - most configuration moved to CSS @theme
 */
module.exports = {
  // Content paths for scanning (if auto-detection doesn't work)
  content: [
    "./src/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  
  // Minimal plugins for v4 - remove potentially incompatible ones
  plugins: [],
}
