# 🌍 ЛОКАЛИЗАЦИЯ ОБЯЗАТЕЛЬНА

## КРИТИЧЕСКИ ВАЖНО:
- НИКАКОГО hardcoded текста для пользователей!
- ВСЕ UI тексты через next-intl: useTranslations('ComponentName')
- Поддерживаемые языки: en, ua

## СТРУКТУРА ПЕРЕВОДОВ:
- Страницы: messages/pages/{page-name}/en.json + ua.json
- Компоненты: messages/components/{component-name}/en.json + ua.json
- Общие элементы: messages/components/common/ (кнопки, ошибки)

## ПРОЦЕСС ДЛЯ НОВЫХ КОМПОНЕНТОВ:
1. Создать папку: messages/components/{component-name}/
2. Создать en.json и ua.json с переводами
3. Добавить в src/i18n/request.ts:
   - импорт: import(`../../messages/components/{name}/${locale}.json`)
   - регистрация в messages: ComponentName: componentMessages
4. В компоненте: const t = useTranslations('ComponentName')
5. Заменить все "hardcoded" на {t('key')}

## ПРИМЕРЫ:
❌ НЕ ДЕЛАЙ: <button>Save</button>
✅ ДЕЛАЙ: <button>{t('save')}</button>

❌ НЕ ДЕЛАЙ: <h1>Welcome to Store</h1>  
✅ ДЕЛАЙ: <h1>{t('welcome')}</h1>

## ПРОВЕРКА СУЩЕСТВУЮЩИХ:
- Ищи в messages/components/common/ общие элементы
- Переиспользуй: useTranslations('Common') для кнопок/ошибок
- Создавай новые только для специфичного контента

## КИБЕРПАНК СТИЛЬ:
- Сохраняй: [BRACKETS], CAPS_LOCK, SYSTEM.EXE в переводах
- Техно термины: LOADING..., ERROR.LOG, STATUS: ACTIVE
- Украинский: [СТАТУС.LOG], ЗАВАНТАЖЕННЯ..., СИСТЕМА.EXE

📚 Детальное руководство: docs/I18N_GUIDE.md
